# Minimal bridge configuration: a single camera stream Cortex -> ROS 2.
version: 1

cortex_to_ros2:
  - name: camera_rgb
    cortex:
      topic: "/sensor/camera/rgb"
      type: ImageMessage
    ros2:
      topic: "/camera/image_raw"
